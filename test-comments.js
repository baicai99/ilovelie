// 这是一个计算函数
function calculate() {
    return 1 + 1;
}

/*
这是一个数据处理函数
用来处理用户输入的数据
*/
function processData() {
    return "processed";
}

/**
 * 这是一个算法实现
 * 用来优化性能
 * @param {number} value - 输入值
 */
function optimizeAlgorithm(value) {
    return value * 2;
}

/* 单行的多行注释 */
const variable = "test";

// 创建新对象
const obj = {};

// 遍历数组元素
const arr = [1, 2, 3];

/*
多行注释
包含变量定义
和方法说明
*/
class MyClass {
    // 构造函数初始化
    constructor() {
        this.value = 0;
    }

    // 获取结果数据
    getResult() {
        return this.value;
    }
}
